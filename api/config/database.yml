default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: common_ground_development

test:
  <<: *default
  database: common_ground_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
